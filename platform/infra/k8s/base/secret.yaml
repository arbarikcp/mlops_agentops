# DO NOT commit real secrets here — use Sealed Secrets or External Secrets Operator in prod.
# These values are base64-encoded placeholders for local kind development only.
apiVersion: v1
kind: Secret
metadata:
  name: credit-risk-secrets
  namespace: ml-serving
type: Opaque
data:
  # echo -n 'changeme' | base64
  POSTGRES_PASSWORD: Y2hhbmdlbWU=
  # echo -n 'minioadmin' | base64
  MINIO_SECRET_KEY: bWluaW9hZG1pbg==
  # echo -n 'placeholder-mlflow-token' | base64
  MLFLOW_AUTH_TOKEN: cGxhY2Vob2xkZXItbWxmbG93LXRva2Vu
