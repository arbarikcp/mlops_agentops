apiVersion: v1
kind: Service
metadata:
  name: {{ include "credit-risk.fullname" . }}
  labels:
    {{- include "credit-risk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "credit-risk.selectorLabels" . | nindent 4 }}
